// Placeholder Capacitor config so the backend layout matches the planned mobile app structure.
const config = {
  appId: "com.justengine.wallpaper",
  appName: "Wallpaper Engine",
  webDir: "dist",
  bundledWebRuntime: false
};

export default config;
